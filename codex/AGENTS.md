# General Agreement for AI Agents

For every task that writes, modifies, debugs, reviews, or refactors code, invoke and follow `$coding-discipline`.

Treat repository `AGENTS.md` files as additional project-specific guidance.